ADD_SOLVER_UTILITY(Fld2Tecplot PulseWaveSolver SOURCES Fld2Tecplot.cpp USELIBRARY)

    IF (NEKTAR_USE_MPI)
        TARGET_LINK_LIBRARIES(Fld2Tecplot LINK_PRIVATE ${GSMPI_LIBRARY} ${XXT_LIBRARY})
    ENDIF (NEKTAR_USE_MPI)
